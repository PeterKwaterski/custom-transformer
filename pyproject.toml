[project]
name = "custom-transformer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.3.0",
    "torchtext>=0.18.0",
    "numpy>=2.0.0,!=2.4.0",
]
